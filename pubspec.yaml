name: calljmp
description: 'Calljmp SDK for Flutter'
version: 0.0.1
homepage:

environment:
  sdk: ^3.8.0
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
